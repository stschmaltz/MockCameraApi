// Please see my comment on camera-operations.test.js for why I don't have real tests here

// to be honest, I don't know if I would have written these tests for this interview anyways
// I think they are important, but I already spent too long on this test with an over-architected solution

describe('cameraModel', () => {
  it('can create new CameraModel instance with all fields', () => {});
  it('can not create  when camera_id is not present', () => {});
  it('handles empty images array', () => {});
  it('handles images array with data', () => {});
});
